# تحلیل جامع پروژه LightRAG

## فهرست مطالب
1. [چیست؟ (What)](#چیست-what)
2. [چرا؟ (Why)](#چرا-why)
3. [چگونه؟ (How)](#چگونه-how)

---

## چیست؟ (What)

### تعریف کلی
**LightRAG** یک سیستم پیشرفته و سبک برای **بازیابی افزوده شده با تولید** (Retrieval-Augmented Generation) است که با استفاده از **گراف دانش** (Knowledge Graph) کیفیت پاسخ‌های مدل‌های زبانی بزرگ (LLM) را به طور چشمگیری افزایش می‌دهد.

### ویژگی‌های اصلی

#### 1. سیستم گراف دانش هوشمند
- **استخراج خودکار موجودیت‌ها و روابط**: بدون نیاز به دستکاری دستی، سیستم به صورت خودکار از متن‌های ورودی، موجودیت‌ها (Entities) مانند افراد، سازمان‌ها، مکان‌ها و روابط (Relations) بین آن‌ها را استخراج می‌کند
- **ساختار دوسطحی**: از دو سطح کلیدواژه استفاده می‌کند:
  - **سطح بالا** (High-Level): مفاهیم کلی و الگوهای جهانی
  - **سطح پایین** (Low-Level): موجودیت‌های خاص و اصطلاحات وابسته به زمینه

#### 2. شش حالت جستجوی متنوع
| حالت | کاربرد | نحوه عملکرد |
|------|---------|-------------|
| **Local** | پرسش‌های وابسته به زمینه خاص | تمرکز بر موجودیت‌های خاص و روابط مستقیم آن‌ها |
| **Global** | پرسش‌های کلی و مفهومی | جستجو در روابط سطح بالا و الگوهای کلی |
| **Hybrid** | ترکیبی از Local و Global | ترکیب متوازن از دو روش بالا |
| **Naive** | جستجوی ساده معنایی | فقط بر اساس شباهت برداری، بدون استفاده از گراف |
| **Mix** | جامع‌ترین حالت | ترکیب گراف دانش + جستجوی برداری + بازیابی متن خام |
| **Bypass** | سفارشی‌سازی توسط کاربر | برای موارد پیشرفته و منطق‌های سفارشی |

#### 3. پشتیبانی از ذخیره‌سازی‌های متنوع
سیستم از **14 نوع پایگاه داده مختلف** پشتیبانی می‌کند:

**پایگاه‌های داده گراف:**
- Neo4j (توصیه شده برای محیط تولید)
- PostgreSQL با Apache AGE
- NetworkX (پیش‌فرض، در حافظه)
- MongoDB
- Memgraph

**پایگاه‌های داده برداری:**
- NanoVectorDB (پیش‌فرض)
- FAISS (فیسبوک)
- Milvus
- Qdrant
- PostgreSQL pgvector
- MongoDB Vector Search

**ذخیره‌سازی کلید-مقدار:**
- JSON (فایل محلی)
- Redis
- PostgreSQL
- MongoDB

#### 4. معماری چهار لایه‌ای ذخیره‌سازی
```
┌─────────────────────────────────────┐
│  KV_STORAGE                         │
│  (متن‌های تقسیم‌شده، کش LLM)      │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  VECTOR_STORAGE                     │
│  (بردارهای موجودیت‌ها و روابط)    │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  GRAPH_STORAGE                      │
│  (گراف موجودیت‌ها و روابط)         │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  DOC_STATUS_STORAGE                 │
│  (وضعیت پردازش اسناد)               │
└─────────────────────────────────────┘
```

#### 5. قابلیت‌های پیشرفته

**الف) مدیریت گراف دانش:**
- ایجاد، ویرایش و حذف موجودیت‌ها
- ایجاد، ویرایش و حذف روابط
- ادغام موجودیت‌های مشابه
- درج گراف دانش سفارشی

**ب) پردازش اسناد چندوجهی:**
- یکپارچگی با RAG-Anything برای پردازش PDF، Word، PowerPoint، تصاویر
- استخراج جداول، فرمول‌های ریاضی و محتوای بصری

**ج) ارجاع‌دهی و پیگیری منبع:**
- نگهداری مسیر فایل‌های منبع
- ایجاد ارجاع‌های دقیق به اسناد اصلی

**د) Reranking هوشمند:**
- پشتیبانی از مدل‌های Reranker (Cohere، Jina AI، Aliyun)
- بهبود دقت بازیابی با رتبه‌بندی مجدد نتایج

**ه) رصد و ارزیابی:**
- یکپارچگی با Langfuse برای مشاهده فراخوانی‌های LLM
- چارچوب ارزیابی RAGAS برای سنجش کیفیت
- ردیابی مصرف توکن

---

## چرا؟ (Why)

### مشکلات RAG سنتی

#### 1. **محدودیت جستجوی معنایی ساده**
RAG‌های سنتی فقط از **شباهت برداری** استفاده می‌کنند:
```
پرسش کاربر → تبدیل به بردار → جستجو در بردارهای مشابه → بازیابی متن
```

**مشکلات:**
- نمی‌تواند روابط پیچیده بین مفاهیم را درک کند
- برای پرسش‌های کلی و مفهومی ناکارآمد است
- محدود به تطابق لغوی و معنایی سطحی

**مثال:**
```
پرسش: "چه ارتباطی بین همه شخصیت‌های اصلی داستان وجود دارد؟"
RAG سنتی: فقط متن‌هایی که شامل نام شخصیت‌ها هستند را برمی‌گرداند
LightRAG: کل شبکه روابط را استخراج و تحلیل می‌کند
```

#### 2. **عدم درک ساختار دانش**
- RAG‌های معمولی ساختار سلسله‌مراتبی یا شبکه‌ای دانش را نمی‌فهمند
- نمی‌توانند استدلال چندمرحله‌ای انجام دهند
- برای پرسش‌هایی که نیاز به جمع‌آوری اطلاعات از منابع متعدد دارند، ضعیف هستند

#### 3. **حجم زیاد متن ارسالی به LLM**
- ارسال کل اسناد طولانی به LLM:
  - هزینه بالا (مصرف توکن زیاد)
  - زمان پاسخ‌دهی طولانی
  - کاهش دقت به دلیل context window محدود

### مزایای LightRAG

#### 1. **درک عمیق‌تر از دانش**
```
متن خام
    ↓
[استخراج موجودیت‌ها و روابط]
    ↓
گراف دانش ساختاریافته
    ↓
[جستجوی هوشمند در گراف]
    ↓
پاسخ دقیق‌تر و جامع‌تر
```

#### 2. **کارایی بالاتر**
- **بودجه‌بندی توکن**: محدودیت تعداد توکن‌های ارسالی به LLM
  - موجودیت‌ها: حداکثر 6000 توکن (پیش‌فرض)
  - روابط: حداکثر 8000 توکن (پیش‌فرض)
  - کل: حداکثر 30000 توکن (پیش‌فرض)
- **کش LLM**: ذخیره نتایج قبلی برای جلوگیری از فراخوانی‌های تکراری
- **پردازش موازی**: استفاده از asyncio برای عملیات همزمان

#### 3. **انعطاف‌پذیری بالا**
- **چند مدلی**: پشتیبانی از OpenAI، Ollama، Hugging Face، Azure، Gemini، Anthropic و...
- **چند زبانه**: قابلیت استخراج به زبان‌های مختلف
- **قابلیت توسعه**: معماری پلاگین‌محور برای افزودن storage و LLM جدید

#### 4. **مقیاس‌پذیری**
- **جداسازی داده** (Data Isolation):
  - Namespace: برای جداسازی نمونه‌های مختلف LightRAG
  - Workspace: برای جداسازی داده‌های مختلف در یک نمونه
- **پردازش دسته‌ای**: توانایی پردازش هزاران سند به صورت همزمان
- **ذخیره‌سازی توزیع‌شده**: پشتیبانی از پایگاه‌های داده توزیع‌شده مانند Milvus، Qdrant

#### 5. **نتایج برتر در ارزیابی‌ها**
بر اساس جدول مقایسه در README:

| معیار | NaiveRAG | **LightRAG** | RQ-RAG | **LightRAG** | GraphRAG | **LightRAG** |
|-------|----------|--------------|---------|--------------|----------|--------------|
| جامعیت | 38.8% | **61.2%** | 39.2% | **60.8%** | 50.4% | **49.6%** |
| تنوع | 32.4% | **67.6%** | 30.8% | **69.2%** | 36.0% | **64.0%** |
| توانمندسازی | 42.8% | **57.2%** | 42.4% | **57.6%** | 50.8% | **49.2%** |

LightRAG در مجموع در **60-85%** موارد بهتر از روش‌های دیگر عمل می‌کند.

---

## چگونه؟ (How)

### معماری کلی سیستم

```
┌─────────────────────────────────────────────────────────┐
│                    کاربر (User)                        │
│         درج اسناد ↓        ↓ پرسش از سیستم           │
└─────────────────────────────────────────────────────────┘
                     ↓                    ↓
┌─────────────────────────────┐  ┌──────────────────────┐
│  خط لوله درج                │  │  خط لوله پرسش        │
│  (Insertion Pipeline)       │  │  (Query Pipeline)    │
└─────────────────────────────┘  └──────────────────────┘
                     ↓                    ↓
┌─────────────────────────────────────────────────────────┐
│              LLM (مدل زبانی بزرگ)                      │
│  - استخراج موجودیت و روابط                             │
│  - تولید پاسخ                                           │
└─────────────────────────────────────────────────────────┘
                     ↓                    ↓
┌─────────────────────────────────────────────────────────┐
│          لایه‌های ذخیره‌سازی (4 لایه)                 │
│  KV | Vector | Graph | DocStatus                       │
└─────────────────────────────────────────────────────────┘
```

### 1. فرآیند درج اسناد (Document Insertion)

#### مرحله 1: تقسیم‌بندی متن (Chunking)
```python
سند کامل
    ↓
[chunking_by_token_size()]
    ↓
تکه‌های متنی (Chunks)
├─ اندازه: 1200 توکن (پیش‌فرض)
├─ همپوشانی: 100 توکن
└─ شماره ترتیب: chunk_order_index
```

**کد مرتبط:** `operate.py:99-176`

**الگوریتم:**
1. تبدیل متن به توکن‌ها با استفاده از Tokenizer (پیش‌فرض: Tiktoken)
2. تقسیم به بخش‌های 1200 توکنی با همپوشانی 100 توکن
3. ایجاد metadata برای هر تکه:
   - `content`: متن تکه
   - `tokens`: تعداد توکن‌ها
   - `chunk_order_index`: شماره ترتیبی
   - `source_id`: شناسه یکتا (hash MD5 از محتوا)
   - `file_path`: مسیر فایل منبع

#### مرحله 2: استخراج موجودیت‌ها و روابط

**الف) استخراج موجودیت‌ها** (`extract_entities` در `operate.py:2768`)

```
تکه متن
    ↓
[ارسال به LLM با Prompt ویژه]
    ↓
پاسخ LLM (فرمت استاندارد)
    ↓
[Parse و Validation]
    ↓
موجودیت‌های استخراج‌شده
```

**فرمت خروجی LLM:**
```
entity<|#|>نام_موجودیت<|#|>نوع_موجودیت<|#|>توضیحات
```

**مثال واقعی:**
```
entity<|#|>Google<|#|>ORGANIZATION<|#|>Google is a multinational technology company
entity<|#|>Sundar Pichai<|#|>PERSON<|#|>CEO of Google and Alphabet Inc.
```

**Gleaning (بهبود تکراری):**
- سیستم می‌تواند چندین بار (پیش‌فرض 1 بار) از LLM بخواهد موجودیت‌های بیشتری پیدا کند
- در هر مرحله، نتایج قبلی به عنوان زمینه ارسال می‌شوند

**ب) استخراج روابط** (`_handle_single_relationship_extraction` در `operate.py:451`)

**فرمت خروجی:**
```
relation<|#|>موجودیت_مبدا<|#|>موجودیت_مقصد<|#|>کلیدواژه‌ها<|#|>توضیحات<|#|>وزن
```

**مثال:**
```
relation<|#|>Sundar Pichai<|#|>Google<|#|>leads manages<|#|>Sundar Pichai is the CEO of Google<|#|>2.0
```

**ویژگی‌های مهم:**
- روابط N-ary به روابط دوتایی (binary) تبدیل می‌شوند
- خود-روابط (self-relations) حذف می‌شوند
- وزن روابط محاسبه می‌شود (پیش‌فرض 1.0)

#### مرحله 3: پردازش و ادغام

**الف) Validation و Sanitization:**
```python
# operate.py:379-527
برای هر موجودیت:
    ✓ نام خالی نباشد
    ✓ نوع معتبر باشد
    ✓ توضیحات خالی نباشد
    ✓ حذف کاراکترهای خاص و نرمال‌سازی
```

**ب) ادغام با گراف موجود** (`merge_nodes_and_edges`):
```
موجودیت جدید
    ↓
آیا در گراف وجود دارد?
    ├─ بله → ادغام توضیحات
    │         ├─ افزودن source_id جدید
    │         ├─ به‌روزرسانی file_path
    │         └─ خلاصه‌سازی (اگر طولانی شده باشد)
    └─ خیر → افزودن به گراف
```

**خلاصه‌سازی شرطی:**
- زمانی که تعداد قطعات توضیحات بیش از 8 باشد
- یا زمانی که کل توضیحات بیش از 1200 توکن باشد
- LLM توضیحات را خلاصه می‌کند تا حجم کاهش یابد

#### مرحله 4: ذخیره‌سازی

**الف) ذخیره متن‌های تکه‌شده:**
```python
# KV_STORAGE
chunks_vdb["full_docs"][doc_id] = {
    "content": سند_کامل,
    "source_id": doc_id,
    "file_path": مسیر_فایل
}

chunks_vdb["text_chunks"][chunk_id] = {
    "content": متن_تکه,
    "tokens": تعداد_توکن,
    "chunk_order_index": شماره_ترتیب,
    "source_id": chunk_id,
    "full_doc_id": doc_id
}
```

**ب) ذخیره بردارهای embedding:**
```python
# VECTOR_STORAGE
entities_vdb.upsert([
    {
        "id": entity_id,
        "embedding": vector,
        "metadata": {...}
    }
])

relationships_vdb.upsert([...])
chunks_vdb.upsert([...])
```

**ج) ذخیره گراف:**
```python
# GRAPH_STORAGE
graph_storage.upsert_node(
    node_id=entity_name,
    node_data={
        "entity_type": نوع,
        "description": توضیحات,
        "source_id": source_ids,
        "file_path": file_paths
    }
)

graph_storage.upsert_edge(
    source_node=entity1,
    target_node=entity2,
    edge_data={
        "weight": وزن,
        "description": توضیحات,
        "keywords": کلیدواژه‌ها
    }
)
```

### 2. فرآیند پرسش (Query Pipeline)

#### مرحله 1: استخراج کلیدواژه‌ها

```python
پرسش کاربر
    ↓
[get_keywords_from_query()]
    ↓
کلیدواژه‌های دو سطحی
├─ High-Level: مفاهیم کلی
└─ Low-Level: موجودیت‌های خاص
```

**کد مرتبط:** `operate.py:3225`

**مثال:**
```
پرسش: "چه ارتباطی بین Google و مدیران آن وجود دارد؟"

کلیدواژه‌های High-Level:
- "organizational structure"
- "corporate leadership"
- "management relationships"

کلیدواژه‌های Low-Level:
- "Google"
- "Sundar Pichai"
- "CEO"
- "executives"
```

#### مرحله 2: جستجو بر اساس حالت (Mode-Based Search)

**حالت Local** (`operate.py:3472`):
```
Low-Level Keywords
    ↓
[جستجوی برداری در entities_vdb]
    ↓
top_k موجودیت
    ↓
[بازیابی یال‌های مرتبط از گراف]
    ↓
زیرگراف موجودیت‌محور
```

**حالت Global** (`operate.py:3480`):
```
High-Level Keywords
    ↓
[جستجوی برداری در relationships_vdb]
    ↓
top_k رابطه
    ↓
[بازیابی موجودیت‌های متصل]
    ↓
زیرگراف رابطه‌محور
```

**حالت Mix** (پیشرفته‌ترین):
```
               پرسش
                ↓
    ┌──────────┼──────────┐
    ↓          ↓          ↓
Local      Global    Naive
Search     Search    Vector
    ↓          ↓          ↓
    └──────────┼──────────┘
               ↓
       [Round-Robin Merge]
               ↓
        نتایج ترکیبی
```

#### مرحله 3: ساخت Context (Unified Token Control)

**معماری 4 مرحله‌ای** (`_build_query_context` در `operate.py:4049`):

```
Stage 1: Pure Search
├─ موجودیت‌ها (از Local)
├─ روابط (از Global)
└─ تکه‌های متنی (از Naive)
    ↓
Stage 2: Token Truncation
├─ max_entity_tokens: 6000
├─ max_relation_tokens: 8000
└─ max_total_tokens: 30000
    ↓
Stage 3: Chunk Merging
└─ ادغام Round-Robin از 3 منبع
    ↓
Stage 4: Context Building
└─ ساخت متن نهایی برای LLM
```

**الگوریتم Round-Robin Merge** (`operate.py:3816`):
```python
result = []
sources = [entity_chunks, relation_chunks, vector_chunks]

while any(sources):
    for source in sources:
        if source:
            result.append(source.pop(0))
            if len(result) >= limit:
                return result
```

**مزایا:**
- توزیع عادلانه از هر منبع
- جلوگیری از تسلط یک منبع
- تضمین تنوع در context

#### مرحله 4: تولید پاسخ

```
Context
    ↓
[ساخت Prompt نهایی]
    ↓
[ارسال به LLM]
    ↓
پاسخ نهایی
    ↓
[افزودن ارجاعات (اختیاری)]
```

**فرمت Prompt:**
```
System Prompt: راهنمای پاسخ‌دهی

Context:
- Entity: [موجودیت‌های مرتبط]
- Relation: [روابط مرتبط]
- Text: [تکه‌های متنی مرتبط]

User Question: [پرسش کاربر]

Generate Response...
```

**قابلیت Streaming:**
```python
async for chunk in rag.aquery(query, stream=True):
    print(chunk, end="")
```

### 3. مثال جریان کامل

#### درج سند:
```python
import asyncio
from lightrag import LightRAG, QueryParam
from lightrag.llm.openai import gpt_4o_mini_complete, openai_embed

async def main():
    # راه‌اندازی
    rag = LightRAG(
        working_dir="./rag_storage",
        llm_model_func=gpt_4o_mini_complete,
        embedding_func=openai_embed
    )
    await rag.initialize_storages()

    # درج سند
    document = """
    گوگل یک شرکت فناوری چندملیتی است که توسط
    لری پیج و سرگئی برین در سال 1998 تاسیس شد.
    سوندار پیچای از سال 2015 مدیرعامل گوگل است.
    """

    await rag.ainsert(document)

    # پرسش
    result = await rag.aquery(
        "چه کسانی گوگل را تاسیس کردند؟",
        param=QueryParam(mode="hybrid")
    )

    print(result)

    await rag.finalize_storages()

asyncio.run(main())
```

#### جریان داخلی:
```
1. Chunking:
   document → ["گوگل یک شرکت...", "سوندار پیچای..."]

2. Extraction:
   موجودیت‌ها:
   - Google (ORGANIZATION)
   - Larry Page (PERSON)
   - Sergey Brin (PERSON)
   - Sundar Pichai (PERSON)

   روابط:
   - Larry Page --founded--> Google
   - Sergey Brin --founded--> Google
   - Sundar Pichai --CEO_of--> Google

3. Storage:
   KV: متن‌های تکه‌شده
   Vector: embeddings موجودیت‌ها و روابط
   Graph: شبکه روابط
   DocStatus: وضعیت: COMPLETED

4. Query:
   "چه کسانی..." → keywords: [Google, founders, established]
   → Search: موجودیت Google + روابط founded
   → Context: Larry Page و Sergey Brin با رابطه founded
   → LLM Response: "گوگل توسط لری پیج و سرگئی برین تاسیس شد..."
```

### 4. نکات پیشرفته

#### الف) بهینه‌سازی عملکرد

**1. پردازش موازی:**
```python
# تنظیم تعداد درج‌های موازی
rag = LightRAG(
    ...,
    max_parallel_insert=4  # پیش‌فرض: 2
)
```

**2. کش LLM:**
```python
rag = LightRAG(
    ...,
    enable_llm_cache=True,  # فعال‌سازی کش
    enable_llm_cache_for_entity_extract=True
)
```

**3. بودجه‌بندی توکن:**
```python
param = QueryParam(
    mode="mix",
    top_k=60,  # تعداد موجودیت/رابطه
    chunk_top_k=20,  # تعداد تکه‌های متنی
    max_entity_tokens=6000,
    max_relation_tokens=8000,
    max_total_tokens=30000
)
```

#### ب) Reranking برای دقت بالاتر

```python
from lightrag.rerank import cohere_rerank

rag = LightRAG(
    ...,
    rerank_model_func=cohere_rerank
)

result = await rag.aquery(
    query,
    param=QueryParam(
        mode="mix",  # توصیه می‌شود با reranker
        enable_rerank=True
    )
)
```

**فرآیند Reranking:**
```
نتایج اولیه (200 تکه)
    ↓
[مدل Reranker امتیازدهی می‌کند]
    ↓
مرتب‌سازی بر اساس امتیاز
    ↓
انتخاب top_k (20 تکه برتر)
```

#### ج) جداسازی داده (Multi-tenancy)

```python
# Workspace برای جداسازی پروژه‌ها
rag1 = LightRAG(
    working_dir="./storage",
    workspace="project_A"
)

rag2 = LightRAG(
    working_dir="./storage",
    workspace="project_B"
)

# داده‌های project_A و project_B کاملاً جدا هستند
```

**پیاده‌سازی در Storage:**
- PostgreSQL: ستون workspace برای فیلتر
- MongoDB: پیشوند workspace در نام collection
- Neo4j: Label-based isolation
- Qdrant: Payload-based partitioning

#### د) مدیریت گراف دانش

**ایجاد موجودیت:**
```python
entity = rag.create_entity("Google", {
    "description": "A technology company",
    "entity_type": "ORGANIZATION"
})
```

**ایجاد رابطه:**
```python
relation = rag.create_relation("Sundar Pichai", "Google", {
    "description": "CEO of Google",
    "keywords": "leads manages",
    "weight": 2.0
})
```

**ادغام موجودیت‌ها:**
```python
rag.merge_entities(
    source_entities=["AI", "Artificial Intelligence"],
    target_entity="Artificial Intelligence",
    merge_strategy={
        "description": "concatenate",
        "entity_type": "keep_first"
    }
)
```

**حذف بر اساس سند:**
```python
await rag.adelete_by_doc_id("doc-12345")
# حذف هوشمند:
# - موجودیت‌هایی که فقط در این سند هستند حذف می‌شوند
# - موجودیت‌های مشترک حفظ و بازسازی می‌شوند
```

### 5. معماری فایل‌ها

```
LightRAG/
├─ lightrag/
│  ├─ lightrag.py          # کلاس اصلی LightRAG
│  ├─ operate.py           # استخراج، پرسش، جستجو
│  ├─ base.py              # رابط‌های ذخیره‌سازی
│  ├─ prompt.py            # قالب‌های LLM
│  ├─ types.py             # مدل‌های داده
│  ├─ constants.py         # تنظیمات پیش‌فرض
│  ├─ utils.py             # توابع کمکی
│  ├─ namespace.py         # مدیریت namespace
│  ├─ llm/                 # درایورهای LLM
│  │  ├─ openai.py
│  │  ├─ ollama.py
│  │  ├─ gemini.py
│  │  └─ ...
│  ├─ kg/                  # پیاده‌سازی Storage
│  │  ├─ neo4j_impl.py
│  │  ├─ postgres_impl.py
│  │  ├─ milvus_impl.py
│  │  └─ ...
│  └─ api/                 # سرور REST API
│     ├─ lightrag_server.py
│     └─ routers/
├─ examples/               # مثال‌های کاربردی
├─ tests/                  # تست‌های واحد
└─ reproduce/              # اسکریپت‌های ارزیابی
```

### 6. الگوهای طراحی کلیدی

#### الف) Strategy Pattern (الگوی استراتژی)
```python
# انتخاب storage بر اساس تنظیمات
storage_class = STORAGES[storage_type]
storage = storage_class(**config)
```

#### ب) Factory Pattern (الگوی کارخانه)
```python
# ساخت انواع مختلف LLM
def create_llm_func(provider):
    if provider == "openai":
        return gpt_4o_mini_complete
    elif provider == "ollama":
        return ollama_model_complete
    ...
```

#### ج) Decorator Pattern (الگوی تزیین‌کننده)
```python
@wrap_embedding_func_with_attrs(
    embedding_dim=1536,
    max_token_size=8192
)
async def embedding_func(texts):
    return await openai_embed(texts)
```

#### د) Observer Pattern (الگوی ناظر)
```python
# ردیابی وضعیت پردازش اسناد
doc_status_storage.update_status(
    doc_id,
    DocProcessingStatus.EXTRACTING
)
```

### 7. بهترین شیوه‌های استفاده

#### 1. انتخاب LLM مناسب
```
استخراج (Indexing):
├─ حداقل 32B پارامتر
├─ Context: 32-64K توکن
└─ مثال: Qwen3-30B، GPT-4o-mini

پرسش (Query):
├─ قوی‌تر از مدل استخراج
└─ مثال: GPT-4o، Claude Sonnet
```

#### 2. انتخاب Embedding مناسب
```
توصیه شده:
├─ BAAI/bge-m3 (چندزبانه)
├─ text-embedding-3-large (OpenAI)
└─ ابعاد بالا برای دقت بیشتر

⚠️ نکته مهم:
- پس از انتخاب، نمی‌توان مدل را تغییر داد
- تغییر مدل نیاز به حذف و بازسازی دارد
```

#### 3. انتخاب حالت پرسش
```
Local:
└─ پرسش‌های خاص درباره موجودیت‌ها

Global:
└─ پرسش‌های کلی و مفهومی

Hybrid:
└─ ترکیبی از دو حالت بالا

Mix (توصیه شده):
└─ جامع‌ترین، به خصوص با Reranker
```

#### 4. تنظیم پارامترها
```python
# برای پرسش‌های پیچیده
param = QueryParam(
    mode="mix",
    top_k=60,           # بیشتر برای پوشش بهتر
    chunk_top_k=20,     # تعادل بین دقت و سرعت
    enable_rerank=True  # بهبود دقت
)

# برای سرعت بالا
param = QueryParam(
    mode="local",
    top_k=20,
    chunk_top_k=10,
    enable_rerank=False
)
```

### 8. یکپارچگی‌های پیشرفته

#### الف) Langfuse برای مانیتورینگ
```bash
# نصب
pip install lightrag-hku[observability]

# تنظیم .env
LANGFUSE_SECRET_KEY="..."
LANGFUSE_PUBLIC_KEY="..."
LANGFUSE_ENABLE_TRACE=true
```

**قابلیت‌ها:**
- ردیابی تمام فراخوانی‌های LLM
- محاسبه هزینه و مصرف توکن
- تحلیل تاخیر (latency)
- دیباگ prompts و responses

#### ب) RAGAS برای ارزیابی
```python
from lightrag.evaluation import eval_rag_quality

metrics = eval_rag_quality(
    rag_instance=rag,
    test_queries=queries,
    ground_truths=answers
)

print(f"Context Precision: {metrics['context_precision']}")
print(f"Faithfulness: {metrics['faithfulness']}")
print(f"Answer Relevancy: {metrics['answer_relevancy']}")
```

#### ج) RAG-Anything برای Multimodal
```python
from raganything import RAGAnything

# یکپارچگی با LightRAG موجود
rag_multi = RAGAnything(
    lightrag=rag_instance,
    vision_model_func=gpt_4o_vision
)

# پردازش PDF با تصاویر و جداول
await rag_multi.process_document_complete(
    file_path="report.pdf",
    output_dir="./output"
)
```

### 9. عیب‌یابی رایج

#### مشکل 1: AttributeError: __aenter__
```python
# ❌ اشتباه
rag = LightRAG(...)
await rag.ainsert(text)  # خطا!

# ✅ درست
rag = LightRAG(...)
await rag.initialize_storages()
await rag.ainsert(text)
```

#### مشکل 2: تغییر Embedding Model
```bash
# پاک کردن داده‌های قبلی
rm -rf ./dickens/*

# حفظ کش LLM (اختیاری)
cp ./dickens/kv_store_llm_response_cache.json /tmp/
rm -rf ./dickens/*
cp /tmp/kv_store_llm_response_cache.json ./dickens/
```

#### مشکل 3: حافظه پر (Out of Memory)
```python
# کاهش بودجه توکن
rag = LightRAG(
    ...,
    chunk_token_size=800,  # کاهش از 1200
    llm_model_max_async=2   # کاهش همزمانی
)

# یا استفاده از مدل کوچک‌تر
llm_model_name='gemma2:2b'  # به جای مدل‌های بزرگ
```

---

## نتیجه‌گیری

### خلاصه قابلیت‌ها

**LightRAG** یک سیستم RAG پیشرفته است که:

1. **چیست:** سیستم بازیابی افزوده با گراف دانش
2. **چرا:** برتری 60-85% نسبت به RAG های سنتی
3. **چگونه:** استخراج هوشمند موجودیت‌ها، 6 حالت پرسش، 14 نوع storage

### کاربردهای عملی

```
🔹 سیستم‌های پرسش و پاسخ سازمانی
🔹 تحلیل اسناد و گزارش‌های پیچیده
🔹 استخراج دانش از متن‌های غیرساختاریافته
🔹 چت‌بات‌های هوشمند با درک عمیق زمینه
🔹 موتورهای جستجوی معنایی پیشرفته
🔹 سیستم‌های توصیه مبتنی بر دانش
```

### نقاط قوت کلیدی

✅ **معماری قابل توسعه**: پلاگین‌محور و منعطف
✅ **عملکرد بالا**: بهینه‌سازی شده برای مقیاس‌های بزرگ
✅ **چندزبانه**: پشتیبانی از زبان‌های مختلف
✅ **مستندسازی جامع**: بیش از 1900 خط README
✅ **جامعه فعال**: بیش از 15000 ستاره در GitHub

### منابع بیشتر

- 📄 مقاله علمی: [arXiv:2410.05779](https://arxiv.org/abs/2410.05779)
- 💻 مخزن GitHub: [HKUDS/LightRAG](https://github.com/HKUDS/LightRAG)
- 📦 PyPI: `pip install lightrag-hku`
- 💬 Discord: [جامعه LightRAG](https://discord.gg/yF2MmDJyGJ)
- 📚 آموزش: [LearnOpenCV Guide](https://learnopencv.com/lightrag)

---

**تاریخ تحلیل:** 2025-12-27
**نسخه پروژه:** بر اساس commit fd9f0f4
